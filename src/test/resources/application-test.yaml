spring:
    datasource:
        password: test #? Setting up Docker from Test-Containers dependency
        username: test #? Setting up Docker from Test-Containers dependency
    jpa:
        properties:
            hibernate:
                ddl-auto: create-drop #? See application.yaml